@model GrupoAOX.Estagio.Application.ViewModel.TransferenciaViewModel

@{
    ViewBag.Title = "Novo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card mb-2 mt-4">
    <div class="card-header">
        <span class="h2">Ordem de Expedição</span>
    </div>

    <div class="card-body">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div id="erros">

        </div>

        <input type="hidden" name="categoriaId" id="categoriaId" value="@ViewBag.CategoriaId" />
        <div class="form-group">
            <div class="col-lg-4">
                @Html.LabelFor(model => model.NumeroDocumento, htmlAttributes: new { @class = "col-form-label" })
            </div>
            <div class="col-lg-3">
                <input type="text" id="NumeroDocumento" name="NumeroDocumento" value="@ViewBag.NumDocumento" class="form-control" readonly="readonly" />
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-2">
                <label class="col-form-label">Etiqueta</label>
            </div>
            <div class="col-md-12">
                <div class="input-group mb-3">
                    <input type="text" id="Etiqueta" name="Etiqueta" class="form-control" placeholder="Informe a Etiqueta" />
                    <div class="input-group-append">
                        <span class="input-group-text"><i class="fas fa-search"></i></span>
                    </div>
                </div>
            </div>
        </div>

        <hr />

        <div class="col-md-12">
            <table class="table table-striped table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Id</th>
                        <th>Etiqueta</th>
                        <th>OP</th>
                        <th>Fitec</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="lotes"></tbody>
            </table>
        </div>
    </div>
    <div class="card-footer">
        <a id="btnVisualizarOrdemExpdicao" target="_blank" class="btn btn-lg btn-primary btnTransferencias" style="display:none;">Visualizar Ordem de Expedição</a>
        @Html.ActionLink("Novo", "Novo", "OrdemExpedicao", null, new { @class = "btn btn-lg btn-warning btnTransferencias mt-1", @style = "display:none;", id = "btnNovoOrdemExpedicao" })
        @Html.ActionLink("Voltar", "Index", "OrdemExpedicao", null, new { @class = "btn btn-lg btn-success btnTransferencias mt-1", @style = "display:none;", id = "btnVoltar" })
        <a class="btn btn-lg btn-success btnTransferencias" id="btnTransferir">Transferir</a>
    </div>
</div>

<div class="modal" id="modal-ordemExpedicao" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Transferindo</h5>
            </div>
            <div class="modal-body d-flex justify-content-center" style="height: 230px">
                <img src="~/images/ajax-loader-200px.gif" />
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/OrdemExpedicao")
}