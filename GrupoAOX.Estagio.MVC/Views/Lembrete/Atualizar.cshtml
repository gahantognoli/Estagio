@model GrupoAOX.Estagio.Application.ViewModel.LembreteViewModel

@{
    ViewBag.Title = "Atualizar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card mb-3 mt-4">
    <div class="card-header">
        <span class="h2">Lembrete</span>
    </div>
    @using (Html.BeginForm("Atualizar", "Lembrete", FormMethod.Post, new { id = "form-lembrete" }))
    {
        <div class="card-body">
            @Html.HiddenFor(model => model.LembreteId)
            <div class="form-group">
                <div class="col-lg-4">
                    <label class="col-form-label">Transferencia Id</label>
                </div>
                <div class="col-lg-3">
                    <input type="text" id="TransferenciaId" name="TransferenciaId" value="@Model.TransferenciaId" class="form-control" disabled="disabled" />
                </div>
            </div>

            <div class="form-group">
                <div class="col-lg-4">
                    <label class="col-form-label" for="NumeroDocumento">Número Documento</label>
                </div>
                <div class="col-lg-3">
                    <input type="text" id="NumeroDocumento" name="NumeroDocumento" value="@Model.Transferencia.NumeroDocumento" class="form-control" disabled="disabled" />
                </div>
            </div>

            <div class="form-group">
                <div class="col-lg-4">
                    @Html.LabelFor(model => model.Descricao, htmlAttributes: new { @class = "col-form-label" })
                </div>
                <div class="col-lg-12">
                    <textarea class="form-control" rows=6 name="Descricao" id="Descricao">@Model.Descricao</textarea>
                </div>
            </div>

            <hr />
            <h3>Lotes</h3>

            <div class="col-md-12">
                <table class="table table-striped table-sm">
                    <thead class="thead-dark">
                        <tr>
                            <th>Id</th>
                            <th>Etiqueta</th>
                            <th>OP</th>
                            <th>Fitec</th>
                        </tr>
                    </thead>
                    <tbody id="lotes">
                        @foreach (var item in Model.Transferencia.Lotes)
                        {
                            <tr>
                                <td>@item.ApontamentoProducaoId</td>
                                <td>@item.Etiqueta</td>
                                <td>@item.OrderProducao</td>
                                <td>@item.Produto</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    <div class="card-footer">
        @Html.ActionLink("Voltar para Lista", "Index", null, new { @class = "btn btn-secondary" })
        <input type="submit" value="Salvar" class="btn btn-success" onclick="$('#form-lembrete').submit();" />
    </div>
</div>
