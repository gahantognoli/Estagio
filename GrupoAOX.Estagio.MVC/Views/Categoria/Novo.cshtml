@model GrupoAOX.Estagio.Application.ViewModel.CategoriaViewModel

@{
    ViewBag.Title = "Novo";
}

<div class="card mb-2 mt-4">
    <div class="card-header">
        <p class="h2">Nova Categoria</p>
    </div>

    <div class="card-body">
        @using (Html.BeginForm("Novo", "Categoria", FormMethod.Post, new { id = "form-create" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <div class="form-group">
                <div class="col-md-2">
                    @Html.LabelFor(model => model.Descricao, htmlAttributes: new { @class = "col-form-label" })
                </div>
                <div class="col-md-6">
                    @Html.EditorFor(model => model.Descricao, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Descricao, "", new { @class = "text-danger" })
                </div>
            </div>
        }
    </div>
</div>

<p>
    @Html.ActionLink("Voltar para Lista", "Index", null, new { @class = "btn btn-secondary" })
    <input type="submit" value="Cadastrar" class="btn btn-success" onclick="$('#form-create').submit();" />
</p>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
